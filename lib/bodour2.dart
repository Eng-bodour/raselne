import 'package:flutter/material.dart';
import 'dart:io';
class Bodour2 extends StatelessWidget {
  const Bodour2({Key? key}) : super(key: key);

  get() {}
  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(8.0),
      child: const Text('Bodour2'),
    );
  }
}
